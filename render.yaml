services:
  - type: web
    name: promptcraft-engine
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - fromGroup: promptcraft-secrets
